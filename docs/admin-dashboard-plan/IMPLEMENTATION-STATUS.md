# Implementation status

This document records implementation evidence for the admin-dashboard plan. It is intentionally not a production-completion claim.

## E14 local update — support email delivery feedback (2026-09-25 Asia/Tbilisi)

- Added additive migration `20260924203707_support_delivery_event_mapping.sql` and the `support-delivery-webhook` Edge Function. Brevo transactional events correlate through the outbox UUID and provider message ID, accept only allowlisted delivery statuses, deduplicate repeated events, and persist only minimal delivery metadata. The admin health card separates provider-reported recipient outcomes from API acceptance and fails closed when the migration is absent or no provider events have arrived.
- Removed the unsupported email-footer promise to reply by email; customer replies are currently saved through the support conversation. Inbound email threading is not implemented and still requires separate provider inbound-parse/MX setup.
- Local verification: all 99 source migrations replayed in isolated PostgreSQL 17, including correlation, duplicate, mismatch, RLS/grant, and aggregate-privacy assertions; all 1,380 unit tests, lint, production build, repository hygiene, Supabase function checks, and Chromium support QA pass. The latest browser run has zero console/page errors, text/non-text contrast violations, or overflow. Evidence: [`evidence/20260925-e14-support-delivery-feedback-local.md`](evidence/20260925-e14-support-delivery-feedback-local.md).
- Fresh read-only Management API checks using the existing project-local credential found eight visible projects in one organization; ResumeATS is `ACTIVE_HEALTHY`, with 93 production migrations through `20260924134252_admin_currency_separated_mrr_snapshot` and 31 deployed functions. The E14 migration is not applied and `support-delivery-webhook` is not deployed; the existing support notification worker is deployed. No production migration/function/configuration change, push, or deployment was made.
- E14 remains in progress: production migration/function/webhook configuration and an actual provider delivery event are unverified; inbound threading, unsubscribe/preferences verification, and the rest of E14's acceptance gates remain open.

## E14 local update — customer support email preferences (2026-09-25 Asia/Tbilisi)

- Added additive migration `20260924220306_support_email_notification_preferences.sql`. Authenticated customers can enable or disable email notices for support replies; their in-app conversation remains available either way. The worker excludes opted-out users and rechecks the current preference and recipient immediately before each send. A correlated Brevo unsubscribe turns notices off unless a newer user choice exists; duplicate callbacks do not undo a later re-enable. The admin health summary exposes only an aggregate suppressed count.
- The migration was applied incrementally to the existing local Supabase database; no reset. The isolated PostgreSQL 17 replay passed all 102 source migrations and the preference/RLS/service-role, suppression, worker preflight, callback-deduplication, and aggregate-health assertions. All 1,388 unit tests passed, as did lint, build, repo hygiene, Supabase Edge Function checks, and `git diff --check` (only existing LF/CRLF normalization warnings).
- Chromium support/admin QA passed the full local journey: guests see no account preference; signed-in users can disable, reload, verify persistence, and re-enable email; worker/API/support/admin/accessibility checks passed with zero console/page errors, zero contrast findings, and no overflow. Evidence: [`evidence/20260925-e14-support-email-preferences-local.md`](evidence/20260925-e14-support-email-preferences-local.md).
- Remaining: production migration and support API/worker/webhook release, Brevo webhook configuration and a real unsubscribe/delivery callback proof, plus the separately scoped inbound-email domain/MX/parser and spoof-resistant conversation binding. E14 and the wider plan are not complete. No production write, push, or deployment was made.

## E14 local update — legacy contact inquiry provenance links (2026-09-25 Asia/Tbilisi)

- Added migrations `20260924212123_support_legacy_inquiry_links.sql` and `20260924214805_support_legacy_link_operator_index.sql`, a support-operator API action, and an explicit historical-inquiry panel. Only an existing customer conversation whose Auth email is confirmed can surface exact normalized-email matches. An AAL2 support operator must explicitly link each inquiry; source content and timestamps remain in `contact_inquiries`, links record the operator/time, linked inquiries cannot be claimed by another conversation, and nothing is copied into `support_messages`.
- Local verification: all 101 source migrations replay in disposable PostgreSQL 17; the persistent local Supabase stack was advanced through its nine pending migrations with no reset; 1,384 unit tests pass; repository hygiene, Supabase function type checks, lint, production build, and Chromium support QA pass. The browser flow verified mixed-case email matching, link success, transcript separation, guest no-auto-match, zero console/page errors, zero contrast findings, and no overflow. Local advisors returned INFO-only findings; the new linker FK is indexed, while no-policy RLS is intentional. Evidence: [`evidence/20260925-e14-legacy-contact-inquiry-links-local.md`](evidence/20260925-e14-legacy-contact-inquiry-links-local.md).
- Production remains read-only and unchanged: the last Management API snapshot showed 93 migrations and 31 deployed functions; the local-only legacy-inquiry migration is not applied there. The existing project-local credential was used only to list the account's eight visible projects; listing success does not establish every write permission.
- E14 remains in progress; this completes only the local legacy-inquiry linking slice. Production migration/deployment and the other E14 provider, inbound-threading, unsubscribe/preferences, and delivery-verification gates remain open.

## Latest E13 local update — explicit support presence (2026-09-25 Asia/Tbilisi)

- The support inbox no longer marks a new operator available on mount. It reads the existing TTL-backed status first, defaults missing/expired presence to offline, reports read failures as unknown, refreshes heartbeats only for confirmed Available/Away states, and immediately records an explicit Offline choice.
- The failing-before browser check and full local support journey now pass: new operator offline, explicit Available/Offline writes accepted, 503 keyboard targets, zero contrast findings, overflow, console errors, or page errors. All 1,377 unit tests, lint, build, repo hygiene, Supabase function checks, and migration script checks pass. Details: [`evidence/20260925-e13-support-presence-local.md`](evidence/20260925-e13-support-presence-local.md).
- No production migration/function/settings change, push, or deployment was made. This does not prove named staffing, production presence, response coverage, or alert delivery.

## E13 local follow-up — support queue unread counts (2026-09-25 Asia/Tbilisi)

- Added additive migration `20260924201815_support_queue_unread_counts.sql` to return per-operator unread customer/guest message counts after the operator's read cursor, and an unread badge that only renders for a valid positive server count. Agent/system messages do not count; the UI fails closed without the migration.
- The isolated PostgreSQL 17 replay passed all 98 source migrations and exact unread/read-cursor fixtures. Chromium support QA passed against the existing migration-absent local database without inventing counts; the full 1,377-test suite, lint, build, repo hygiene, Supabase function checks, and diff/syntax checks passed. Evidence: [`evidence/20260925-e13-support-unread-count-local.md`](evidence/20260925-e13-support-unread-count-local.md).
- Relative to the last read-only production snapshot (93 migrations at 2026-09-24 19:54 UTC), this checkout now contains 98 migration files and five local-only migrations. That production snapshot was not refreshed, and nothing was applied, pushed, or deployed.

## Latest E13 local update — daylight-saving-safe support SLA (2026-09-24 19:54 UTC)

- Reproduced an A13 defect in `support_add_business_minutes`: wall-clock arithmetic overstated/understated real staffed minutes across DST transitions. Additive migration `20260924193925_support_sla_dst_elapsed_minutes.sql` now counts elapsed instants and resolves repeated autumn and skipped spring business-hour boundaries. The private resolver is inaccessible to API and worker roles.
- The disposable PostgreSQL 17 replay passes all 97 local migrations and exact spring/fall, repeated-open/repeated-close, skipped-open/skipped-close and helper-privilege assertions. `npm test` passes 1,377/1,377; lint, build, repository hygiene, Edge Function checks and migration script syntax pass. The persistent local Supabase database was not reset or migrated.
- A fresh read-only Management API check at 19:54 UTC confirms production remains at 93 migrations while this checkout has 97; four local-only migrations remain unapplied, including this SLA fix. No production change, push or deployment was made. Details: [`evidence/20260924-e13-support-sla-dst-local.md`](evidence/20260924-e13-support-sla-dst-local.md).

## E16 local update — Support operations and runtime limits (2026-09-24)

- Admin Settings now includes bounded Support AI usage telemetry, an aggregate-only email queue-health card, and plainly labeled response-token/reply caps. The worker enforces those two limits and escalates capped conversations to humans without a provider call; the database kill switch gates claims. The Admin API also blocks both direct enablement and rollback to an enabled revision while the runtime kill switch is off, with direct behavior tests for deny-before-RPC and safe disable rollback. Daily/monthly USD budget fields are explicitly documented as unenforced until a provider cost contract and atomic cap mechanism are verified. Email configuration shows value presence only; queue metrics omit recipient/message data, and provider API acceptance is not called recipient delivery. Validation passed: 1,377 unit tests, lint, build, repository hygiene, Edge Function type-check, all 97 source migrations replayed in disposable PostgreSQL 17 including a guarded AI turn-cap escalation fixture (human-queue state, no AI reply), stale-completion cancellation after customer handoff, and the DST boundary fixtures; Chromium support/admin QA reported 518 keyboard targets and no contrast, overflow, console, or page errors. Existing local Supabase state was not reset or migrated; browser QA verified the migration-required fallback. No production change, push, or deployment was made. Details: [`evidence/20260924-e16-support-ai-usage-telemetry-local.md`](evidence/20260924-e16-support-ai-usage-telemetry-local.md), [`evidence/20260924-e16-support-email-queue-health-local.md`](evidence/20260924-e16-support-email-queue-health-local.md), and [`evidence/20260924-e16-support-ai-runtime-limits-local.md`](evidence/20260924-e16-support-ai-runtime-limits-local.md).

## Production snapshot — analytics backend and website follow-up deployed (2026-09-24; inventory at 19:02 UTC)

- The 2026-09-24 19:02 UTC read-only refresh found 93 production migrations matching the first 93 local files, no remote-only versions, and three local-only migrations: `20260924143655_admin_ga4_report_cache.sql`, `20260924174548_admin_support_ai_usage_telemetry.sql`, and `20260924180906_admin_support_email_delivery_health.sql`. Migrations 90–91 add consent-limited 7-day resume activation and timezone-aware exact-day D7/D30 retention cohorts; migration 92 adds privacy-minimized daily event aggregates; migration 93 adds a currency-separated subscription base-price run-rate preview. The three local-only migrations pass isolated PostgreSQL 17 replay but are not applied in production. Incomplete cohort and run-rate coverage remains explicitly labeled. The previously deployed migration `20260924010401` remains unchanged.
- The same refresh confirms all 31 deployed Edge Functions are `ACTIVE`, including `admin-api` v24 and `support-api` v3; the 29 local source directories all correspond to deployed functions. Two additional deployed functions have no source directory in the main checkout: `create-customer-portal-session` v28 (`verify_jwt=false`) and `gemini-proxy` v30 (`verify_jwt=true`). Their read-only downloaded snapshots were reviewed and quarantined outside the deploy tree; neither was removed or changed in production. Current source has no caller for either function. See [`evidence/20260924-e00-current-inventory.md`](evidence/20260924-e00-current-inventory.md).
- The latest Vercel production deployment tracks `main` commit `6c0a79c461f6482ee67d30359f8181a1aee5f71d` and is `READY` at [`ats-friendly-resume-builder-e9b15285c-giorgikemos-projects.vercel.app`](https://ats-friendly-resume-builder-e9b15285c-giorgikemos-projects.vercel.app); `vercel inspect https://www.resumeats.cv` resolved the canonical alias to a ready production deployment. The live HTTP/asset boundary audit at `2026-09-24T15:45:15.929Z` passed with zero failures: 8 public routes, 21 private-route response checks, 55 assets, and 3 expected Edge Function 405/CORS probes. This does not establish authenticated workflows.
- Fresh read-only Management API project inventory at `2026-09-24T15:41:43Z` found eight accessible projects and no separately named ResumeATS staging project; the target `ATS-FRIENDLY-RESUME-BUILDER` is `ACTIVE_HEALTHY`. No project was created. See [`evidence/20260924-e00-current-inventory.md`](evidence/20260924-e00-current-inventory.md).
- Current local checks: 1,377 unit tests, lint, production build, repo hygiene, Supabase function type-check, and all 96 isolated PostgreSQL 17 migration replays pass. E15 separately tests system-message exclusion and untrusted context handling; the replay also proves an authenticated customer handoff cancels a pending AI run as stale without inserting a late reply. Production AI remains gated pending provider approval and human adversarial review (details: [`evidence/20260924-support-ai-boundary-local.md`](evidence/20260924-support-ai-boundary-local.md) and [`evidence/20260924-local-migration-replay.md`](evidence/20260924-local-migration-replay.md)). E16 has Chromium proof for feedback-owner assignment/unassignment; Admin Settings reports bounded AI queue freshness without claiming provider connectivity; the admin job response omits raw failure/action reasons and provider IDs, and synthetic browser fixtures verify safe Retry/Reconcile/completed-job controls (details: [`evidence/20260924-e16-feedback-owner-assignment-local.md`](evidence/20260924-e16-feedback-owner-assignment-local.md)). The GA4 connector rejects timezone-mismatched reports and cache entries; focused GA4/backend tests pass 45/45. E12 has bounded scanner-response handling, worker malformed-verdict tests, and clean-only signed-download HTTP evidence in [`evidence/20260924-attachment-scan-local.md`](evidence/20260924-attachment-scan-local.md). The 18-step fixture flow, support/admin flows across Chromium/Firefox/WebKit, 200% zoom checks, and responsive builder checks pass with their documented synthetic-data boundaries. The latest Chromium support/admin rerun verifies support isolation/handoff/resolution/CSAT, AAL1 denial, and AAL2 access; it reported 518 keyboard targets, no text/non-text contrast findings, no overflow, and zero console/page errors (this rerun did not exercise browser zoom). On the live site, a pointer click on the home heading shows no focus outline; keyboard Tab still visibly focuses “Skip to main content.” Screenshots are in [`../product-audit/2026-09-24/`](../product-audit/2026-09-24/).
- Earlier authorized migration/function releases used the user-provided Desktop token in process memory; the current project-local token was used only for read-only project inventory and was not displayed, moved, or committed. No production mutation accompanied the local GA4/timezone work. Vault and unrelated secrets/Auth settings were not changed. Worker/config values, scheduler jobs, owner TOTP enrollment, authenticated production role/session checks, provider sandbox checks, monitoring, and backup/restore drills remain owner/operations gates.

## Earlier production snapshot — before the follow-up release (2026-09-24)

- Commits through `14c6a20` are pushed to `origin/main`. Vercel deployment `dpl_M4uShkZDEH9R31Bm2yXAYh74Mspm` is `READY` on `www.resumeats.cv` and `resumeats.cv`.
- Production has 86 applied migrations and no history drift; `admin-api` v20 and `support-api` v2 are `ACTIVE`, retaining `verify_jwt=false` because their handlers perform bearer/session validation. Three pending local-only migrations cover paid-conversion cohorts, audited QA exclusions, and support queue read-before-assignment; none has been applied to production. The deployed migration `20260924010401` remains unchanged; its follow-up correction is isolated in additive migration `20260924090331`.
- The latest production HTTP audit at `2026-09-24T09:43:44.994Z` returned zero failures across 8 public routes, 21 private-route response checks, 55 referenced assets, and 3 Edge Function GET/CORS checks. A fresh E00 read-only refresh confirms production 86 / local 89 migrations, zero remote-only versions, 31 active deployed functions, 15 missing worker/config values, and no configured Supabase Cron jobs. The project-local PAT in ignored `.env.supabase.local` can read the target project and its inventory; the earlier HTTP 403 was from the user-scope token, while Supabase MCP still uses a denied connector identity. Current evidence: [`evidence/20260924-e00-access-refresh.md`](evidence/20260924-e00-access-refresh.md).
- Current local checks: 1,340 tests pass; lint, production build, repo hygiene, and Edge Function type-check pass; all 91 local migrations replay on a disposable PostgreSQL 17 instance with paid/activation/retention cohorts, QA exclusions, support AAL2/cursor, RLS, owner-privilege, and analytics event-constraint proofs. Replay details and test boundaries: [`evidence/20260924-local-migration-replay.md`](evidence/20260924-local-migration-replay.md). The consented AI lifecycle events now cover resume/provider-proxy, keyword, application-answer, Auto-Apply, and Gmail classification paths and assert no prompt, resume, email, or generated content is persisted; AI pricing stays explicitly unpriced. Fresh Chromium, Firefox, and WebKit `support-local-qa` runs pass customer/guest isolation, handoff, resolution, CSAT, AAL1 denial/AAL2 access, synthetic QA exclude/re-include, and support read-before-assignment; the latest runs report 494/496/496 keyboard targets respectively, 22 text, 22 non-text, and 110 status-tone audits with zero findings or browser/page errors. The latest Chromium support/admin run also asserts the Product retention D7/D30 cards render, reports 496 keyboard targets, zero contrast findings and no browser/page errors. The Chromium 200% zoom run passed all 22 checks. The 18-step fixture browser flow passes with no page errors, console errors, or blocked requests; the 390px compact-consent height stays at or below 145px, and the tablet-width builder clipping regression is fixed locally. The current 32-route smoke test passes. Home and Learn heading clicks show no pointer focus frame; keyboard focus remains visible. Screenshots: `../product-audit/2026-09-24/production-home-hero-after-text-click-final.png` and `../../output/playwright/consent-layout-20260924/compact-consent-390.png`.
- The post-release 200% run's DOM bounds fit (viewport 632px; document 624px; body 625px; heading text range ends at 379px). Its 632×402 DPR-2 screenshot did not visually agree and was rejected as screenshot evidence; see the release evidence note.
- GitHub CI run `35956959516` and CodeQL run `35956959221` completed successfully for `14c6a20`; CI used the randomized PostgreSQL service port. The pending local-only support/analytics API/UI changes and three migrations are not part of that release.
- Earlier analytics deployment and migration dry-run attempts used a token without ResumeATS access and returned HTTP 403. The newly identified project-local token supports Management API reads, but no production support/analytics migration, API update, secret, or Auth change was made; the account currently has no separate ResumeATS staging project and release gates remain open.
- The documented project-local PAT read-only database endpoint runs as `supabase_read_only_user` with `transaction_read_only=on` and captured current production relation grants, RLS/policies, view options, function execution metadata, and default-ACL owners. A separate CLI SQL-query route reported `current_user=postgres`; it was used once for a metadata-only `SELECT`, then excluded from future read-only checks. No data rows or production objects were written. E00's full role/session behavior matrix remains open. See [`evidence/20260924-production-readonly-grants-inventory.md`](evidence/20260924-production-readonly-grants-inventory.md) and [`evidence/20260924-e00-access-refresh.md`](evidence/20260924-e00-access-refresh.md).
- A follow-up read-only PostgreSQL 17.6 catalog query confirmed `postgres` cannot inherit or `SET ROLE supabase_admin`; all current public relations, sequence, and 134 functions are `postgres`-owned. Since PostgreSQL applies defaults for the active object-creating role, the separate `supabase_admin` ACL is a conditional risk if the deployment actor changes, not evidence of current app-object exposure. The replay now asserts `postgres` is the creator and all 89 migrations pass. No defaults were changed; require role/owner verification on staging releases. See the inventory evidence.
- This release does not complete production readiness: the owner has not enrolled a verified TOTP factor, 15 worker/config secret names are absent, and there are no scheduler jobs. Production authenticated role/session journeys, provider sandbox checks, monitoring, and backup/restore drills remain open. See [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).

## Completed in the current working tree

- Light-first admin shell with persisted Light / Dark / System preference.
- Responsive admin navigation with every currently implemented admin area exposed as a single canonical navigation model.
- Honest overview cards that show unavailable values instead of inventing zeroes.
- Admin mutation idempotency headers, durable operation receipt migration, and audit-write failure handling.
- Future `public` tables, sequences, and functions created by migration owner `postgres` no longer inherit API-role grants; local replay probes and linked-production default-ACL checks verify grants remain opt-in. The default-ACL migration was applied at 80/80; after the later six session/AAL2 migrations, current local/remote parity is 86/86. See [`evidence/20260924-default-public-grants.md`](evidence/20260924-default-public-grants.md).
- High-risk admin mutations now require a signed AAL2/MFA claim in addition to the database membership role; read-only dashboard actions remain available for status and setup.
- Admin API requests and selected direct support/admin RPCs require a live Auth session matching the verified JWT `session_id`; service-role attachment access also verifies the exact user/session pair. The predicates fail closed on missing claims, revoked/expired sessions, or database errors. The reviewed migrations and `admin-api`/`support-api` functions are now deployed; production authenticated role/session behavior remains unverified. See [`evidence/20260924-admin-session-guard.md`](evidence/20260924-admin-session-guard.md), [`evidence/20260924-direct-support-session-guard.md`](evidence/20260924-direct-support-session-guard.md), and [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).
- Admin Settings now provides an authenticated Supabase TOTP enrollment and verification flow, with no factor secret persisted by the application.
- Admin Settings now exposes support-AI integration health plus an AAL2/idempotent safety-settings workflow: kill switch, bounded token/turn limits, and daily/monthly budgets are written through a service-only audited transaction; enablement is refused when provider/worker secrets are absent, and secret values never reach the browser.
- Support-AI settings now have database-owned revisions, recent history, and a bounded worker/admin circuit-breaker control; terminal provider failures pause new claims for five minutes and an AAL2 admin can clear the pause through an audited action.
- Manual premium access routed through the provider-separated entitlement reconciler.
- Manual access grants have their own expiry/revoke ledger; active Stripe, PayPal, and manual grants reconcile without erasing another valid source.
- Entitlement-owned AI limit column with Stripe, PayPal, and manual provider support.
- First-party analytics event table, allowlist, sensitive-field rejection, deduplication, account-created trigger, and verified Stripe/PayPal purchase events.
- E08 paid-conversion and QA-exclusion cohort work is deployed. Owner-only AAL2 workflows manage QA category and effective scope, audit exclude/include changes, and invalidate cohort review; isolated PostgreSQL fixtures verify 2/10 = 20%, zero and maturing denominators, ignored manual grants/test/zero/late payments, exclusion-review invalidation, and deletion-quality blocking. Actual measurement remains unavailable until enough post-coverage cohorts mature, both providers reconcile, QA exclusions are reviewed, and deletion-safe aggregate handling exists. Event-count ratios remain diagnostics, not user conversion.
- E08 reporting now includes Asia/Tbilisi calendar-midnight date filters, exact half-open UTC ranges, timezone metadata in UI/CSV, consent-gated `resume_exported` events after successful export dispatch, versioned 7-day activation and exact-calendar-day D7/D30 retention, and privacy-minimized daily aggregates. The daily aggregate migration and API/dashboard changes are deployed (migration 92 and `admin-api` v23); local replay verifies 4/10 activation, 3/10 D7 and D30 observed fixtures, cache invalidation, zero-fill, and service-only access. Final cohort rates remain withheld because product-event coverage is incomplete and some events require optional consent. A local-only server-side GA4 acquisition report now has a service-role-only 15-minute cache, bounded stale fallback, explicit disconnected state when credentials are absent, and fail-closed property-timezone/cache validation; the associated migration, function, and UI have not been deployed. Focused GA4/backend tests pass 45/45. Live GA4 access remains unverified pending a dedicated read-only service account, property permission, API enablement, server secret configuration, and a successful live report. Consent-compatible full-population activation, mature first-party cohorts, and authenticated production drill-down remain open.
- Entitlement-aware admin reads with expiry handling and a read-only subscriptions ledger.
- Subscriptions now include source-backed Stripe and PayPal webhook receipt history with processed, skipped, failed, and in-flight states; unsupported provider actions remain explicitly disabled.
- Billing now has provider/environment-specific subscription and transaction projection tables, service-only upsert/record RPCs, Stripe invoice/subscription projection hooks, PayPal subscription/transaction projection hooks, and an admin reconciliation view that keeps missing migrations and unavailable rows explicit.
- Billing reconciliation now has a provider/environment lease table, serialized run claim/complete/fail RPCs, a bounded internal worker that re-fetches Stripe and PayPal state through ownership checks, paginates bounded PayPal transaction history, preserves sandbox/live projection labels, and exposes an admin health view that distinguishes failed observations from webhook receipts.
- Billing actions now have a disabled-by-default provider capability matrix, amount/currency limits, durable idempotent intent and attempt/receipt tables, an MFA-protected admin preview/intent boundary, and fail-closed unsupported responses; provider sandbox execution remains gated until the capability policy and worker credentials are reviewed.
- Billing action execution now has a secret-bound, leased worker boundary for reviewed Stripe/PayPal cancel, resume, refund, and customer-approval plan-change flows; provider uncertainty records `pending_reconciliation`, unsupported operations never become false successes, and the worker remains disabled by the capability matrix until sandbox review.
- AI & Jobs now has an operations panel with profile usage totals, allowance totals, and separate auto-apply job/run status counts; safe retry, cancel, and reconciliation-request controls are audited and idempotent, while unknown provider cost/latency and employer-facing submission remain unavailable.
- AI & Jobs status reporting now separates the actual `auto_apply_jobs` lifecycle states from `auto_apply_runs` execution states, with local browser coverage for both panels.
- Accessible admin action dialog replacing browser `prompt` / `confirm`, with Escape close and focus restoration.
- Support conversation core with customer/guest identity separation, durable messages, immutable events, read cursors, delivery outbox, idempotent operations, and service-only guest-token functions.
- Customer support widget and operator inbox wired to separate customer replies and internal notes, with searchable queue, assignment/CAS takeover, priority/tags triage, read acknowledgement, resolution/reopen, and handoff actions; the customer widget now reads the server-backed hours, queue-capacity, and live-presence context before submission and labels offline/open/queued states truthfully.
- Support edge function body/rate guards and hashed, memory-only guest session tokens in the browser client.
- Support attachment foundation now uses a private 10 MB JPEG/PNG/PDF bucket, signed uploads, ownership-bound finalization, quarantine state, service-only scan transitions, and five-minute authorized downloads; text chat remains usable while scanning is unavailable.
- Support follow-up email delivery now has an agent-message trigger, service-only leased outbox claim/complete/retry/dead-letter RPCs, and a bounded Brevo worker with server-only credentials and HTML escaping; guest conversations without a verified email remain in-app only.
- Resolved support conversations now accept one durable, identity-bound customer rating/comment from either an authenticated customer or an active guest session; the customer widget and operator inbox expose the resulting feedback without mixing it into the transcript.
- Feedback operations now expose bounded operator tags, aggregate category/tag themes, and an idempotent improvement backlog with sanitized summaries, source-feedback linkage, priority/status/impact, and active-operator ownership checks; raw transcript text is never copied automatically.
- Support knowledge now has private versioned articles, source references, draft creation, reviewed publish/rollback actions, a manager-only admin panel, and a service-only published reader for future AI retrieval; no AI provider is enabled by this slice.
- Support AI now has a disabled-by-default settings row, leased run/usage tables, audited AAL2-protected kill-switch/budget settings, revision history with rollback, a service-only guarded commit that rejects stale handoff/message state, and a timeout-bounded structured-output worker; no provider, scheduler, or production enablement is configured.
- Support attachment scanning now has a service-only `SKIP LOCKED` lease/claim contract and an internal fail-closed worker endpoint: it validates size/signature, calls an explicitly configured scanner, retries transient failures, and never marks an upload clean without a scanner verdict.
- Bounded customer-directory projection with profile-sync trigger, indexed server search, stable cursor ordering, role-checked access, and a paginated admin “Load more” path.
- Role-checked customer detail reads now show bounded account, usage, billing-source, support-count, and first-party activity metadata without exposing private resume/application content.
- Date-bounded first-party analytics endpoint and admin view with event counts, derived rates, freshness/source metadata, and unavailable-state handling.
- Admin Analytics now offers a date-bounded CSV export of the same aggregate metrics and rates, with server-side allowlisting and formula-injection protection; it contains no customer rows or private content.
- Stripe entitlement events retain provider subscription IDs; PayPal webhook deliveries use a durable retryable event inbox.
- Resume and application creation now enter first-party analytics through database triggers with idempotent event keys.
- Successful resume exports emit a privacy-safe Google Analytics `resume_exported` event; application creation and exports remain free of resume content and identifiers.
- Optional GA/Vercel analytics now fail closed until an explicit visitor choice, persist Accept/Decline state, support withdrawal from the Privacy page, clear analytics cookies on withdrawal, and keep first-party server-confirmed purchase events separate from visitor analytics.
- GA4 now emits the recommended `sign_up` event after Supabase reports a new identity, with only `method: email`; duplicate-account responses and visitors without granted analytics consent are not sent to GA.
- E07 source coverage is mapped to concrete GA4, Vercel, client RPC, Edge Function, and database-trigger callsites in [`evidence/analytics-emission-coverage.json`](evidence/analytics-emission-coverage.json), with a test that fails when known helper callsite counts or direct-emitter source markers drift. Client analytics requires explicit consent, permits only the two production customer hosts, and excludes `/admin` routes. `checkout_started` records consented provider intent separately from successful GA4 `begin_checkout`; AI lifecycle outcomes now cover resume/provider-proxy, keyword, application-answer, Auto-Apply, and Gmail classification with local consent-gated, content-free server instrumentation, safe failure classes, and opaque attempt IDs. Application creation now emits status-only GA4 after the database insert succeeds; first-party creation remains captured once by its stable-keyed database trigger. The support-AI worker remains on separate operational usage records. GA4 purchase IDs remain provider-prefixed opaque analytics-event UUIDs. Remaining gaps include production deployment and processed-event verification.
- The consented signup event is deployed on `main` and the canonical production site; `sign_up` is configured as a GA4 key event with no default monetary value. Release evidence: [`evidence/20260924-release-verification.md`](evidence/20260924-release-verification.md).
- Guest support sessions survive same-tab reload through session storage and reset cleanly when the short-lived token expires.
- Local browser QA now covers the disposable guest/operator support journey through handoff, takeover, separated notes and replies, resolution, guest follow-up, and CSAT with strict local-origin binding.
- Privacy lifecycle foundation now provides role-checked, idempotent export/deletion requests, explicit legal/accounting/security/support holds, cancellation for pending requests, and a service-only deletion-claim RPC; no immediate Auth deletion or fake download is exposed.
- Privacy exports now have a private 50 MB JSON bucket, service-only leased claim/complete/retry/expiry RPCs, an allowlisted export worker that excludes credentials, Gmail tokens, internal notes, and raw provider webhook payloads, and five-minute admin-signed download URLs for ready exports. Deletion invalidates pending/ready export objects before account removal.
- Privacy deletion requests now require a separate owner approval boundary, snapshot active Stripe/PayPal entitlements into an audited provider-cancellation review boundary, and fail closed until evidence is recorded and billing reconciliation marks provider rows inactive. The separate service-only deletion worker removes private storage and customer-owned data, unlinks retained accounting/audit aggregates, deletes Auth only after the data step succeeds, and completes only after Auth confirmation; neither the admin API nor review action calls a payment provider.
- Admin ban and deletion requests now fail closed for targets with an active team membership and require a separate owner-controlled revoke; the deletion transaction independently rechecks membership at execution time. Isolated PostgreSQL replay now proves simultaneous reciprocal owner revokes and demotions each leave one active owner and one audit receipt, and deletion only continues after the explicit team revoke. Production role/session and hosted concurrency verification remain open. See [`evidence/20260924-active-admin-action-guard.md`](evidence/20260924-active-admin-action-guard.md).
- Owner-only team administration now supports audited role changes for active members, with database-serialized role/revoke transactions, server-side role validation, self-change protection, and last-owner safeguards. Email-only access is represented as a seven-day, server-enforced invitation that can be reclaimed after expiry by a new owner action; email delivery and broader team settings remain separate work.
- Support Settings now includes a versioned, service-only hours/routing contract with validated timezone, business days, queue guard, first-response target, automatic-routing flag, revision history, audited owner/admin updates, operator presence TTLs, DST-safe elapsed business-minute deadlines, and truthful queue SLA status; it does not claim provider delivery.
- Admin invitations now have a service-only, expiring email outbox with trigger-based enqueueing, bounded leases, retry/dead-letter states, Brevo idempotency, and visible queued/sent/failed delivery state; delivery stays disabled until the provider secret and application URL are configured.
- Admin user-directory role compatibility now uses the verified JWT role accessor and is covered by both service-role and authenticated-owner local RPC replay checks.
- Authenticated local browser QA covers the disposable owner flow, source-backed overview data, HTTP 200 admin API responses, Light-first default, persisted Dark mode, mobile drawer/Escape behavior, overflow safety, full Tab traversal across all 11 admin sections in both Light and Dark (latest clean counters: 480 in Chromium and Firefox), actual 200% per-tab browser zoom across all 11 sections in both themes, and zero findings in 22 rendered text-contrast and 22 interactive-control boundary audits. Focus indicators meet the local >=3:1 check and no zoomed surface overflows. The closed mobile drawer is hidden from both pointer and keyboard interaction after its close transition. The latest support-specific WebKit browser flow passes; earlier request-cancellation failures were avoided by exercising route changes through the UI. Screenshots are retained in `evidence/`.
- The five shared status-badge tones are now tested in a local-only fixture across all 22 admin section/theme scans in Chromium, Firefox, and WebKit (110 tone checks per engine, zero findings); visible application badges also retain text labels. The customer support widget's Light and Dark surfaces pass separate rendered text and close-control contrast checks in each engine. These latest palette, widget, and QA changes remain local and are not deployed.
- The contrast and zoom fixes documented in the earlier local verification are now in production; Chromium, Firefox, and WebKit support-flow browser runs pass, with 200% zoom explicitly verified in Chromium. Broader status/graphic contrast and assistive-technology reviews remain open. Details: [`evidence/20260924-admin-control-contrast.md`](evidence/20260924-admin-control-contrast.md).

## Evidence collected

- 2026-09-24 default API-object privilege hardening: 80-migration isolated replay, local Supabase ACL probe and schema lint passed; authorized linked production migration applied and default ACLs verified. Parity was 80/80 at that release; current parity is 86/86 after the later session/AAL2 migration release. Details: [`evidence/20260924-default-public-grants.md`](evidence/20260924-default-public-grants.md).
- 2026-09-24 linked-production capability and security-advisor snapshot: [`evidence/20260924-production-capability-audit.md`](evidence/20260924-production-capability-audit.md). This is read-only evidence and does not close the authenticated role-behavior, worker, scheduler, or backup/restore gates.
- 2026-09-24 follow-up read-only production grants/default-ACL inventory using the project's PAT: [`evidence/20260924-production-readonly-grants-inventory.md`](evidence/20260924-production-readonly-grants-inventory.md). It refreshes relation/function counts and distinguishes the `postgres` migration-owner defaults from `supabase_admin` defaults; direct role/session behavior remains unverified.
- Original 2026-09-24 15:03 UTC read-only E00 migration/function/secret inventory and recovered deployed-only function source review: [`evidence/20260924-e00-current-inventory.md`](evidence/20260924-e00-current-inventory.md).
- 2026-09-24 19:02 UTC read-only capability refresh: production had 93 migrations versus 96 local, all 31 deployed functions active, 15 worker/config values absent by name, and no `pg_cron`/`pg_net` scheduler. That snapshot's three local-only migrations passed isolated PostgreSQL 17 but remained unapplied; no remote settings or rows were changed. Evidence: [`evidence/20260924-e00-current-inventory.md`](evidence/20260924-e00-current-inventory.md).
- New 2026-09-24 19:54 UTC migration-only refresh: production remains at 93 migrations; current local count is 97, with four local-only migrations, no remote-only versions, and no production write. The added SLA timezone migration passes the isolated replay; see [`evidence/20260924-e13-support-sla-dst-local.md`](evidence/20260924-e13-support-sla-dst-local.md).
- Fresh 2026-09-23 local verification, support browser run, GA4 funnel finding, and read-only production capability snapshot: [`evidence/20260923-local-verification.md`](evidence/20260923-local-verification.md). This supplements, but does not erase, the dated evidence below.
- 2026-09-24 release check for the consented signup event, live asset, HTTP routes, and ResumeATS GA4 key-event setting: [`evidence/20260924-release-verification.md`](evidence/20260924-release-verification.md).
- 2026-09-24 GA4 dashboard save/reopen evidence, including its conversion-rate metric and current processed-event limitation: [`evidence/20260924-ga4-dashboard.md`](evidence/20260924-ga4-dashboard.md).
- Local-only GA4 acquisition report connector, cache security boundary, test results, and remaining live-credential gate: [`evidence/20260924-ga4-acquisition-local.md`](evidence/20260924-ga4-acquisition-local.md).
- Attachment scanner URL/response bounds, malformed-verdict worker tests, and clean-only signed-download HTTP fixtures: [`evidence/20260924-attachment-scan-local.md`](evidence/20260924-attachment-scan-local.md). Scanner-provider configuration and production parity remain unverified.
- 2026-09-24 E07 analytics emission inventory and source-checked callsite test: [`evidence/analytics-emission-coverage.json`](evidence/analytics-emission-coverage.json). This is local source evidence, not proof of deployed wiring or processed-event delivery.
- 2026-09-24 consent-gated AI lifecycle event contract, privacy boundary, and local verification: [`evidence/20260924-ai-analytics-lifecycle.md`](evidence/20260924-ai-analytics-lifecycle.md). The event migration and function/client wiring remain local-only pending staging/release gates.
- 2026-09-24 production release verification for the session/AAL2 migrations and admin/support functions, latest Vercel release, HTTP/assets, and visual focus check: [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).
- 2026-09-24 direct admin/support Auth-session guard and attachment boundary checks: [`evidence/20260924-direct-support-session-guard.md`](evidence/20260924-direct-support-session-guard.md). Its original 80-migration production snapshot is historical and is superseded by the 86/86 deployment verification in [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).
- 2026-09-24 active administrator ban/deletion request guard and executable fail-closed tests: [`evidence/20260924-active-admin-action-guard.md`](evidence/20260924-active-admin-action-guard.md). The guard shipped in `admin-api` v20; production authenticated role/session behavior and hosted concurrency remain open.
- Fresh 2026-09-24 AAL2 support/operator step-up, Light/Dark viewport and basic accessibility DOM checks across all 11 admin sections, 86-migration local replay, 17-scenario fixture browser suite, 17-route public/auth DOM audit, 32-route public/protected smoke test, and read-only project/function access check: [`evidence/20260924-aal2-support-step-up.md`](evidence/20260924-aal2-support-step-up.md). The production deployment and subsequent live verification are recorded in [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md); no production secrets or Auth factors were changed.
- Follow-up local rendered text and interactive-control contrast audits, plus working-tree test/build verification: [`evidence/20260924-admin-control-contrast.md`](evidence/20260924-admin-control-contrast.md).
- 2026-09-24 follow-up commit `b3cd4ee0a872c862f813f013a5a28df0a6903bfb` is on `origin/main`; CI run `35930967940` passed and Vercel deployment `dpl_C76khBy1Uax9uRwVJQyT5SboQUid` is `READY` with production aliases. `https://www.resumeats.cv` returned HTTP 200. This Vercel release does not apply Supabase migrations or deploy Supabase Edge Functions.
- `npm run check:supabase:functions` — passed.
- `npm run build` — passed.
- `npm run lint` — passed with no warnings after PayPal response/type hardening.
- Latest local `npm test` — passed 1,331/1,331 tests, including consent-gated AI lifecycle coverage for Auto-Apply and Gmail, analytics source-manifest checks, and the broader admin plan contracts.
- Docker Desktop `4.90.0` / Docker Engine `29.7.2` — healthy; the local Supabase core stack starts after the stale Docker AI/runtime state was disabled and rotated into recoverable backups. The optional Vector log collector is disabled locally because it requires insecure Docker TCP `2375`; database-backed application analytics is unaffected.
- `npx.cmd --yes supabase db lint --local --level error` — the earlier local schema lint passed with no schema errors; the latest isolated PostgreSQL replay validates all 91 current local migrations and authorization assertions. The local Supabase database was not reset in this work cycle.
- `npm run test:migration-replay` — passed all 91 current local migrations on an isolated PostgreSQL 17 instance. The replay covers Auth/RPC ownership, quota, concurrency, privacy, support, billing, auto-apply, feedback, paid/activation/retention cohorts, event capture/refund/deletion gates, public `SECURITY DEFINER` grants/search paths, active-session matching/expiry, private AAL2 assurance checks, AAL1 denial/AAL2 acceptance, owner-managed QA exclusion changes, support mark-read before assignment, and service-only boundaries. Retention assertions cover exact local day 7/day 30, account-grain de-duplication, incomplete-coverage rate withholding, timezone boundaries and maturing users. Synthetic Storage fixtures confirm private support/export bucket limits, per-user resume-folder reads/writes, cross-user denial, and no direct authenticated support-object access. This does not exercise managed Auth or Storage HTTP APIs, signed-upload tokens, production role sessions, or production objects. Production remains at 89 migrations; see [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).
- `npm run test:storage:http` — passed against the loopback local Supabase/Edge Function stack. It exercised authenticated and guest support signed uploads, upload-token path binding, cross-user/guest denial, finalize-to-quarantine, declared/actual size mismatch blocking, oversized-prepare denial, private resume-owner access, and direct support-object upload/read denial; all synthetic Auth, conversation, guest-session, and Storage data was cleaned. This test exposed and verified the fix for the handler's outer catch bypassing safe 401/404/422 error mapping; actual wrong-guest, cross-customer, and oversized-prepare responses now return 401, 404, and 422. It does not prove scanner-backed release/download or production behavior.
- `git diff --check` — passed; Git reports only the repository's LF/CRLF normalization warnings.
- `npm run test:website:smoke` with an isolated preview port `4199` — passed for all 30 public/protected routes, including the parameterized Stripe return path, analytics decline, Privacy-page withdrawal, re-acceptance, and safe missing-payment-return states.
- `npx --no-install supabase db push --linked --include-all --skip-vault --yes` — earlier deployments applied 42 and one migration; the 2026-09-24 coordinated release applied the six reviewed session/AAL2 migrations with Vault skipped. A fresh linked check reports 86 local / 86 remote versions.
- `npx --no-install supabase functions deploy --project-ref onuxzcectniowxqtmjpg --use-api --jobs 1` — earlier deployment published all 29 then-local Edge Functions without pruning or changing provider secrets. The 2026-09-24 release targeted only `admin-api` v20 and `support-api` v2; both are active and retain manual JWT/session validation (`verify_jwt=false`).
- Git commit `7b7269a4c5c9ec54cce1d58cbf0cc5869b15b00f` was pushed to `origin/main`; Vercel deployment `JBfPNgTgi4k5km1oTFomnrwGhygJ` completed and was aliased to `https://www.resumeats.cv`.
- `npm run audit:production:http` at `2026-09-09T20:45:58Z` — passed the live route, metadata, asset, and edge-function boundary gate with no failures.
- Production secret-name inventory — Stripe, PayPal, Brevo, and configured AI provider credentials are present; support-AI, attachment-scanner, privacy-worker, billing-worker, and invitation-worker secrets are absent, and no scheduler references are present in the migration source. Those features remain fail-closed rather than being reported as live.
- 2026-09-24 earlier source releases `484c672`, `44e1ab2`, and `b3cd4ee` remain in Git/Vercel history; the latest release and current deployment are recorded in [`evidence/20260924-production-release-final.md`](evidence/20260924-production-release-final.md).

## Not yet verified or released

- The current working tree contains 98 migration files; five are local-only relative to the 93-migration production snapshot: `20260924143655_admin_ga4_report_cache.sql`, `20260924174548_admin_support_ai_usage_telemetry.sql`, `20260924180906_admin_support_email_delivery_health.sql`, `20260924193925_support_sla_dst_elapsed_minutes.sql`, and `20260924201815_support_queue_unread_counts.sql`. The last read-only production secret-name audit found no `GA4_SERVICE_ACCOUNT_JSON`; refresh that inventory before any release. No GA4 migration or function deployment was performed for that slice, and live GA4 data is not yet connected. See [`evidence/20260924-ga4-acquisition-local.md`](evidence/20260924-ga4-acquisition-local.md).
- The 15:03 UTC linked production snapshot was 94 local / 93 production with zero remote-only versions. Subsequent local changes bring the checkout to 98 source migrations, all of which pass isolated PostgreSQL 17 replay. Production authenticated role behavior and the direct REST/RPC/Storage matrix remain unverified.
- The six auth/session/AAL2 guard migrations (`20260923220527_admin_auth_session_guard.sql`, `20260923223745_enforce_active_sessions_for_direct_admin_and_support.sql`, `20260923232233_active_session_private_uuid_lookup.sql`, `20260923232407_enforce_aal2_for_direct_admin_operators.sql`, `20260923233340_move_admin_aal2_predicate_private.sql`, and `20260924010401_harden_support_conversation_read_aal2.sql`) have been applied after local replay and review. The migration-first/function-second rollout is complete; the authenticated production role/session matrix remains open.
- Authenticated production `/admin` visual and routed QA was completed previously and is documented in [`evidence/20260910-production-admin-qa.md`](evidence/20260910-production-admin-qa.md). A comprehensive production role/session matrix across customer, guest, support, billing, admin, owner, revoked, and stale-session states, including direct REST/RPC/Storage behavior, remains open.
- The 19:02 UTC read-only capability refresh confirms 59/59 public tables and 8/8 Storage tables have RLS enabled, 31/31 deployed Edge Functions are active, and the 40-name secret inventory is missing the 15 worker/config values listed in the linked E00 evidence. Secret values were not read. Neither `pg_cron` nor `pg_net` nor scheduler jobs are available. One active owner exists but has no verified TOTP factor. Provider credentials were checked by name/presence only; the full existing-object grant/membership matrix and production row-level behavior remain unverified.
- Full provider sandbox replay/reconciliation/actions, hosted scheduler and alert configuration, configured attachment scanner execution, AI provider evaluation/enablement, invitation email delivery/broader team settings, staged backup/restore and destructive deletion drills, and end-to-end authenticated staging/production journeys remain in progress. Implemented fail-closed boundaries are not evidence that those integrations are live.
- The earlier frontend source commit `fba83dcbdbd9a8c79298674db64407b17e42edd0` had a `READY` production deployment and clean HTTP audit. It is superseded by the latest release recorded above; release success does not close the external integration gates.
