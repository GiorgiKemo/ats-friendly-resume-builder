# AAL2 support/operator step-up — 2026-09-24

## Local implementation and verification

- Direct admin-directory, support-operator, and knowledge-manager authorization now requires both a live Auth session and AAL2. The assurance predicate is private; it is not exposed as a public authorization helper.
- `support-api` verifies the signed user's AAL claim and live session before allowing operator actions. Owner access to their own attachment remains available at AAL1; cross-user operator attachment access requires AAL2.
- Authenticated support-member `read` and `markRead` actions now require AAL2 before their user-scoped RPC runs; ordinary customer reads remain available at AAL1. `node --test tests/supportApiSessionGuard.test.js tests/qaScriptContracts.test.js` passed 33/33, including both sides of that boundary.
- Direct support conversation RPCs now independently verify the caller's live Auth session. Assignment or agent-participant status no longer grants read, cursor, or send access to an AAL1 staff member; assigned AAL2 operators retain access, while ordinary customer AAL1 reads/sends remain available. Start, send, handoff, feedback, read, and mark-read reject stale customer sessions.
- `node --test tests/supportApiSessionGuard.test.js` passed 7/7, including 403 for staff `send`, `read`, and `markRead` at AAL1 and successful customer AAL1/operator AAL2 cases.
- Local TOTP enrollment is enabled only in `supabase/config.toml` for this disposable QA environment. No production Auth setting was changed.
- `npm run test:website:support` passed: guest session recovery/isolation, AAL1 operator denial, local authenticator step-up, AAL2 queue access, handoff, internal-note separation, public reply, resolution, and CSAT. Admin Light/Dark/System behavior, Light persistence across reload, mobile Escape/focus restoration, and horizontal overflow passed at 360, 720, 768, 1024, 1440, and 1920 CSS pixels. No unexpected browser console or page errors.
- After Docker Desktop was confirmed ready (v29.7.2), the local support browser flow was rerun successfully; all core Supabase containers were running, the database/Auth containers were healthy, and Auth health returned HTTP 200. Optional image proxy, analytics, vector, and pooler containers remained stopped.
- The browser flow now enumerates all 11 admin sections, verifies each active navigation state and section heading, and checks one main landmark, one top-level heading, named controls, and unique IDs on every section. This exposed a duplicate admin `<h1>`; the shell label was changed to a styled non-heading without changing its visual treatment. Light and Dark layouts pass at 360, 720, 768, 1024, 1440, and 1920 CSS pixels with no horizontal overflow or unexpected browser errors. The 720 CSS-pixel check is a narrow-viewport proxy, not a browser-level 200% zoom test. This is Chromium evidence, not a full supported-browser, contrast, or assistive-technology signoff.
- Keyboard focus follow-up: browser assertions found transparent indicators on native Analytics date fields and a Settings text input; a later headed-browser check also identified native Settings time fields. `.admin-shell :focus-visible` applies the theme focus token as an important 2px solid outline with 2px offset, and Analytics date plus Settings time wrappers provide visible indicators for Chromium's native date/time controls. The local Chromium flow Tabs through every visible control in all 11 sections in both Light and Dark and verifies focus visibility, indicator width, and at least 3:1 contrast against the nearest opaque ancestor background; the latest fixture run traversed 448 controls.
- Visual focus evidence: [`admin-focus-ring-overview-light-local-2026-09-24T01-58-36-949Z.png`](admin-focus-ring-overview-light-local-2026-09-24T01-58-36-949Z.png) captures the keyboard-focused Overview Open analytics control in the local Development environment.
- Actual browser zoom: `SUPPORT_QA_BROWSER_ZOOM=1` was run in an isolated, disposable Chromium profile. Chrome reported per-tab zoom 2.0 and a 632px CSS viewport; document/body widths were 624/625px and all measured headings remained within the viewport. Every admin section passed no-overflow and keyboard-focus checks in Light and Dark (22 section/theme checks). This exposed and fixed a mobile drawer height/scroll issue that made theme controls unreachable, plus closed-drawer visibility/pointer behavior that allowed hidden navigation controls into keyboard traversal. Screenshot: [`admin-200-percent-zoom-overview-light-local-2026-09-24T02-43-16-288Z.png`](admin-200-percent-zoom-overview-light-local-2026-09-24T02-43-16-288Z.png).
- The admin header labels development builds as `Development environment`; production builds retain `Live data where connected` so local/staging UI is not presented as production.
- Latest screenshots: [`support-guest-resolved-local-2026-09-24T01-58-36-949Z.png`](support-guest-resolved-local-2026-09-24T01-58-36-949Z.png) and [`support-inbox-local-2026-09-24T01-58-36-949Z.png`](support-inbox-local-2026-09-24T01-58-36-949Z.png).
- After the actual-zoom and keyboard-focus changes, the support browser flow passed the complete local guest/operator, AAL1/AAL2, 11-section theme/viewport, 448-target latest-run traversal, and 22 actual browser-zoom checks; no production code was deployed.
- Latest verification after the closed-drawer and real-zoom changes: `npm test` passed 1,303/1,303; `npm run lint`, `npm run build`, `npm run check:repo`, and `npm run check:supabase:functions` passed. `git diff --check` also passed (only LF/CRLF normalization warnings).
- `npm run test:website:full` passed all 17 authenticated fixture scenarios with zero failures, page errors, console messages, or requests outside the disposable local app/backend origins.
- `npm run audit:accessibility` passed its 17 public/auth/error route DOM checks (one main landmark and h1 per route; named and labelled controls; unique IDs). This route audit does not substitute for a full assistive-technology review of every admin screen.
- `npm run test:website:smoke` passed all 32 public/protected routes on an isolated local preview port.
- `npm run audit:production:http` completed at `2026-09-24T00:11:36Z` against `https://www.resumeats.cv` with `failures: []`; checked public metadata/canonicals, private noindex behavior, live assets, and public function boundaries. This GET-only check reflects the currently deployed site, not the uncommitted AAL2 changes.
- `npx.cmd --yes supabase db lint --local --level error` reported no schema errors. The latest isolated PostgreSQL 17 replay passed all 86 local migrations and the session/AAL2 authorization, directory, and support RPC proofs, including direct assigned-agent read/cursor/send denial at AAL1, stale-customer-session rejection, and AAL2 read/send acceptance. The replay used a disposable loopback-only PostgreSQL container that was stopped and auto-removed; the local Supabase database was not reset.
- `git diff --check` passed; only the repository's existing LF/CRLF normalization warnings were reported.

## Read-only production access and release boundary

- A project-local Supabase Management API token authenticated read-only project and function-list requests. The ResumeATS project (`onuxzcectniowxqtmjpg`) reported `ACTIVE_HEALTHY`; the function list returned 31 deployed functions and `support-api` was `ACTIVE`, version 1.
- These GET requests prove the token can read those Management API resources; they do not prove every account permission, database access, or permission to release changes.
- No production migration, Edge Function, Auth setting, secret, or customer data was changed. The three local migrations dated 2026-09-23 and the updated local `support-api` remain undeployed. Production rollout still requires the production role/session compatibility gate and an explicit release decision.
